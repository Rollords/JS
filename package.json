{
  "name": "repaso",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i input.css -o ./src/assets/css/main.css --watch",
    "build": "npx @tailwindcss/cli -i input.css -o ./src/assets/css/main.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
